include ':app'
include ':data'
include ':domain'
include ':SystemUISharedLib', ':QuickstepCompat', ':QuickstepCompatVQ'

if (hasProperty('buildScan')) {
    buildScan {
        termsOfServiceUrl = 'https://gradle.com/terms-of-service'
        termsOfServiceAgree = 'yes'
    }
}